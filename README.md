# API Сервис Заявок на Займ (Yii2, Nginx, PostgreSQL, Docker)

Этот проект представляет собой простое API для подачи и обработки заявок на займ, реализованное на стеке Yii2, Nginx, PostgreSQL и Docker Compose.

## 🚀 Стек Технологий

* **Бэкенд:** PHP 8.2 / Yii2 Framework
* **Веб-сервер:** Nginx
* **База данных:** PostgreSQL 13
* **Контейнеризация:** Docker Compose

## 📦 Инструкция по Запуску

**Требования:**
* Docker
* Docker Compose
* Composer

---

### 1. Установка Yii2

Проект ожидает, что код Yii2 (включая `vendor`) будет находиться в директории `/app`.

1.  **Склонируйте этот репозиторий:**
    ```bash
    git clone [URL-вашего-репозитория]
    cd loans
    ```

2.  **Установите Yii2 Basic Template в папку `app`:**
    *Если папка `app` уже содержит файлы проекта, но нет папки `vendor`, просто запустите `composer install` внутри `app`.*

    Если папка `app` пустая, используйте Composer, чтобы скачать в нее фреймворк:
    ```bash
    composer create-project --prefer-dist yiisoftware/yii2-app-basic app
    ```

3.  **Перезапишите файлы конфигурации:**
    После установки Yii2, файлы из этого репозитория (`app/config/web.php`, `app/controllers/ApiController.php`, `app/migrations/...` и т.д.) должны быть скопированы в папку `app` с заменой.

4.  **Установите зависимости Composer (если не ставили на шаге 2):**
    ```bash
    cd app
    composer install
    cd ..
    ```

### 2. Запуск Docker-контейнеров

Находясь в корневой папке проекта (`loans/`):

```bash
docker-compose up -d --build